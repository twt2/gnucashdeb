Description: Ensure that account order is preserved when renumbering subaccounts
Author: Sébastien Villemot <sebastien@debian.org>
Bug: https://bugzilla.gnome.org/show_bug.cgi?id=696469
Bug-Debian: http://bugs.debian.org/cgi-bin/bugreport.cgi?bug=472996
Applied-Upstream: commit: r22834
Last-Update: 2013-03-26
---
This patch header follows DEP-3: http://dep.debian.net/deps/dep3/
--- a/src/gnome-utils/dialog-account.c
+++ b/src/gnome-utils/dialog-account.c
@@ -1951,7 +1951,7 @@
     if (response == GTK_RESPONSE_OK)
     {
         gtk_widget_hide(data->dialog);
-        children = gnc_account_get_children(data->parent);
+        children = gnc_account_get_children_sorted(data->parent);
         prefix = gtk_editable_get_chars(GTK_EDITABLE(data->prefix), 0, -1);
         interval =
             gtk_spin_button_get_value_as_int(GTK_SPIN_BUTTON(data->interval));
